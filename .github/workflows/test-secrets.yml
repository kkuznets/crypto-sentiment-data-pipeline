name: Create Secret

on: push

jobs:
  create_secret:
    runs-on: ubuntu-latest
    steps:
    - name: Create secret
      uses: actions/secrets@v1
      with:
        secret-name: MY_SECRET_NAME
        secret-value: 123
